dbt_learn:
  target: dev
  outputs:
    dev:
      type: spark
      method: thrift
      host: spark-thrift
      port: 10000
      schema: default
      threads: 4
      connect_retries: 3
      connect_timeout: 600
